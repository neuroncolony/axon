# axon
Token launcher on pons v2, chain 4663, with pooled AI compute.
The 2 percent creator tax funds a shared compute pool for holders.
Env vars: PORT (server port), AXON_TREASURY (treasury wallet), OPENROUTER_API_KEY (AI spend).
Run: python3 server/server.py
